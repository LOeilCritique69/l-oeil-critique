name: MAJ Bandes Annonces Automatique

on:
  schedule:
    - cron: '15 07 * * *'  # Tous les jours à 07h15 UTC (09h15 heure française)
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    env:
      PAT_TOKEN: ${{ secrets.GH_TOKEN_2 }}

    steps:
      - name: ⬇️ Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python et dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install

      - name: ▶️ Exécuter le script Python
        run: python l_oeil_critique/scripts/scraper_bandes_annonces.py

      - name: 🔁 Commit & push avec token PAT
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add .
          git commit -m "MAJ auto des bandes-annonces" || echo "Aucun changement à commit"
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/LOeilCritique69/l-oeil-critique.git
          git push origin main
